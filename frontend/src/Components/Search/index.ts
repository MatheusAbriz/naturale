import styled from "styled-components";

export const StyledContainerPesquisar = styled.div`
    img{
        width: 1.5rem;
        height: 1.5rem;
        right: 12px;
        top: 4px;
    
        &:hover{
            cursor: pointer;
        }
    };
`